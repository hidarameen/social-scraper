## Packages
recharts | Dashboard analytics charts
framer-motion | Smooth layout transitions and animations
date-fns | Date formatting for logs and timestamps
lucide-react | Icons (standard, but ensuring it's available)
clsx | Class name conditional logic
tailwind-merge | Merging tailwind classes safely

## Notes
Auth is handled via Replit Auth (use-auth hook).
All API requests must include `credentials: "include"`.
Dark mode dashboard style.
